simulate = function(n_trts, n_burn_cycles, n_adaptive_periods,
                    betas, y_sigma, n_chains, n_iter, lag,
                    stabilize = NULL,
                    objective = "maximize",
                    adapt_delta = 0.999,
                    max_treedepth = 15,
                    seed = 1) {

  set.seed(seed)

  # Stopping the RMD CHECK for global variables
  id = NULL



}
